#include<stdio.h>
int main() {
	int A[2][3] = { {1,2,3},{4,5,6} };
	printf("A[0][0]的地址为：%p\nA[0][1]的地址为：%p\n", A[0][0],A[0][1]);
	int b;
	//int *c;
	b = &A[0][1]- & A[0][0];
	printf("%d\n", b);
	//c = &A[0][1];
	if (b = 1) {
		printf("按行排列");
	}
	else {
		printf("按列排列");
	}
	return 0;
}
